port pBody media2

macro makeMedia(name)
   media name
      type: "text/html"
   end
end

macro makeMedia2(mName, mType)
   media mName
      type: mType
   end
end

*makeMedia("media1")
*makeMedia2("media2", "text/html")
