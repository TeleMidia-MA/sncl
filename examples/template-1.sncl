macro makeMedia(id, class, src)
   media id
      class: class
      src: src
   end
end

macro makeContext(id)
   context id
      for i = 1, #photo do
         makeMedia("photo[i]")
      end
   end
end

makeContext("c1")
